# M1 Test Log - 2026-07-31

Branch: `feature/lunar-ble`

## Step 0 - Repo State

Result:

```text
feature/lunar-ble tracking origin/feature/lunar-ble
```

Latest commit before hardware scripting:

```text
dafc629 ci: add platformio firmware build
```

## Step 1 - PlatformIO Availability

Result:

```text
PlatformIO Core, version 6.1.19
```

## Step 2 - Firmware Build

Command:

```bash
pio run
```

Result:

```text
SUCCESS
RAM: 13.7%
Flash: 28.7%
```

## Step 3 - Serial Port Check

Command:

```bash
tools/dev/check_ports.sh
```

Visible ports:

```text
/dev/cu.debug-console
/dev/cu.Bluetooth-Incoming-Port
/dev/tty.Bluetooth-Incoming-Port
/dev/tty.debug-console
```

Result:

```text
[WARN] No likely ESP32 USB serial port found.
```

## Step 4 - Flash Attempt

Command:

```bash
tools/dev/flash_lunar_poc.sh
```

Result:

```text
[ERROR] No ESP32 USB serial port found.
[NEXT] Connect the Freenove ESP32 WROVER and run: tools/dev/check_ports.sh
```

## Current Status

Software side is ready for first hardware flash.

Hardware verification is blocked until the Freenove ESP32 WROVER appears as a USB serial device.

## Board Decision Update

Correction from hardware inspection:

```text
ESP32-S3 WROOM-1 is self-assembled and has a USB-C orientation fault.
It cannot connect over USB and is blocked for M1.
```

New primary board:

```text
Freenove ESP32 WROVER
```

Default PlatformIO environment:

```text
freenove_esp32_wrover
```

Default WROVER build result:

```text
SUCCESS
RAM: 12.1%
Flash: 90.3%
```

Note: 90.3% flash use is acceptable for the M1 proof-of-data firmware, but should be watched before adding larger features.

Available boards named by user:

```text
Freenove ESP32 WROVER
Freenove ESP32 GPIO Expansion Board
ESP32-S3 WROOM-1
```

Decision:

```text
Use Freenove ESP32 WROVER first.
Do not use ESP32-S3 WROOM-1 for M1 because its USB-C connector is assembled incorrectly and cannot connect.
Use GPIO Expansion Board only as wiring breakout.
```

## Next Physical Step

Connect the Freenove ESP32 WROVER via a known data-capable USB cable, then run:

```bash
tools/dev/check_ports.sh
```

Expected next pass signal:

```text
/dev/cu.usbmodemXXXX
```

or another explicit USB serial candidate.

## Step 5 - WROVER Connected

User reported:

```text
WROVER angesteckt, LED leuchtet
```

Port check result:

```text
[WARN] No likely ESP32 USB serial port found.
```

Visible serial ports remained:

```text
/dev/cu.debug-console
/dev/cu.Bluetooth-Incoming-Port
/dev/tty.Bluetooth-Incoming-Port
/dev/tty.debug-console
```

USB tree check did not show a clear Freenove, CH340, CP210x, WCH, or USB serial device.

Interpretation:

```text
The WROVER is receiving power, but no USB data connection is visible to macOS/Codex yet.
```

Next physical action:

```text
Connect USB directly to the WROVER programming port with a known data-capable cable.
Avoid the GPIO expansion board and power-only hubs for this test.
```

## Step 6 - USB 3.0 Retest

User reported:

```text
Moved WROVER connection from USB 2.0 to USB 3.0.
```

Port check result:

```text
[WARN] No likely ESP32 USB serial port found.
```

PlatformIO device list:

```text
/dev/cu.debug-console
/dev/cu.Bluetooth-Incoming-Port
```

Raw serial ports:

```text
/dev/cu.Bluetooth-Incoming-Port
/dev/cu.debug-console
/dev/tty.Bluetooth-Incoming-Port
/dev/tty.debug-console
```

USB tree matched devices:

```text
USB 2.0 BILLBOARD
USB2.0 Hub
USB 2.0 Hub
USB Billboard Device
USB3.0 Hub
AX88179A
```

No clear Freenove, ESP32, CH340, CH341, WCH, CP210x, UART, USB serial, `usbserial`, or `usbmodem` device appeared.

Interpretation:

```text
Changing from USB 2.0 to USB 3.0 did not change enumeration.
The WROVER still receives power but is not visible as a USB data device.
```

Next physical action:

```text
Use a known data-capable USB cable directly into the WROVER programming USB port.
Do not route through the Freenove GPIO Expansion Board for this test.
Do not press BOOT/RESET yet; no USB serial device exists to put into bootloader mode.
```

## Step 7 - USB Hub Constraint

User clarified:

```text
MacBook has no native USB-A port.
The WROVER connection currently runs through a USB hub.
```

Interpretation:

```text
The hub/adapter path is now part of the active diagnosis.
Because the WROVER does not appear in the macOS USB tree, the most likely unresolved causes remain cable data capability, hub data forwarding, adapter path, or board USB-UART hardware.
```

Next physical action:

```text
Keep the WROVER connected directly to its own programming USB connector.
Test a known data-capable Micro-USB cable through the hub.
If possible, test the same hub with another known USB data device.
Preferred clean test: USB-C to Micro-USB data cable, or USB-C to USB-A adapter plus known data Micro-USB cable.
```

## Step 8 - Hub Data Path Confirmed

User clarified:

```text
Hub works.
```

Follow-up port check result:

```text
[WARN] No likely ESP32 USB serial port found.
```

PlatformIO device list:

```text
/dev/cu.debug-console
/dev/cu.Bluetooth-Incoming-Port
```

USB tree still shows the hub path but no WROVER/USB-UART device:

```text
USB2.0 Hub
USB3.0 Hub
AX88179A
```

Interpretation:

```text
The hub is no longer the primary suspect.
The WROVER still does not enumerate as a USB device.
The next most likely causes are a charge-only Micro-USB cable, wrong WROVER-side USB port/path, or defective USB-UART hardware on the board.
Driver installation is only the next step if a USB-UART device appears in the USB tree but no serial port is created.
```

Next physical action:

```text
Replace the Micro-USB cable with a known data-capable cable.
Keep using the WROVER programming USB connector.
After changing only the cable, rerun tools/dev/check_ports.sh.
```

## Step 9 - Data Cable Confirmed and Firmware Flashed

User reported:

```text
Cable changed to USB-C to USB-C.
```

Port check result:

```text
/dev/cu.usbserial-10
Hardware ID: USB VID:PID=1A86:7523 LOCATION=0-1
Description: USB Serial
```

USB tree result:

```text
USB Serial
```

Interpretation:

```text
The previous blocker was the USB data path.
The new USB-C to USB-C cable exposes the WROVER USB-UART bridge successfully.
```

Firmware flash command:

```bash
tools/dev/flash_lunar_poc.sh /dev/cu.usbserial-10
```

Build result:

```text
SUCCESS
RAM: 12.1%
Flash: 90.3%
```

Upload result:

```text
Chip is ESP32-D0WD-V3 (revision v3.1)
MAC: f4:2d:c9:76:64:bc
Hash of data verified.
Hard resetting via RTS pin.
SUCCESS
```

Short serial monitor check:

```text
[BLE] scan started
[BLE] Searching...
```

Tooling cleanup:

```text
Removed unsupported PlatformIO monitor filter `colorize`.
Second short monitor check started without the previous filter warning.
```

Result:

```text
M1 firmware is flashed and running on the Freenove ESP32 WROVER.
Next milestone is live BLE connection to the Acaia Lunar and first weight stream capture.
```

## Step 10 - Acaia Lunar BLE Discovery

User reported:

```text
Acaia is on.
```

First BLE monitor result:

```text
[BLE] found name=ACAIAL-C1EF962 address=00:1c:97:1e:f9:62 rssi=-38 to -42
[BLE] connecting address=00:1c:97:1e:f9:62
[BLE] connected
[BLE] service not found
[BLE] disconnected
```

Interpretation:

```text
The ESP32 BLE radio can see and connect to the real Acaia Lunar AL008.
The fixed service UUID from the first protocol implementation does not resolve on this observed Lunar profile.
```

Firmware response:

```text
Added BLE service discovery after connection.
Added characteristic matching for the newer Acaia command/data UUIDs.
Added legacy 0x2A80 read/write fallback.
Added scan-debug logging for all observed BLE advertisements.
```

Local debug configuration used for hardware diagnosis:

```text
LUNAR_TARGET_MAC=00:1c:97:1e:f9:62
BLE_SCAN_LOG_ALL=1
```

Post-patch build and flash result:

```text
Build: SUCCESS
Flash: SUCCESS
```

Post-patch scan-debug result:

```text
ESP32 sees many nearby BLE advertisements.
No ACAIA/ACAIAL device and no 00:1c:97:1e:f9:62 advertisement appeared during the later scan-debug run.
```

Current status:

```text
ESP32 BLE scan path is verified.
Lunar was verified once by name and address before the service-discovery patch.
The current blocker is that the Lunar is not advertising during the later run.
Next action is Acaia-side Bluetooth state check, not ESP32 USB or firmware flashing.
```

Next physical action:

```text
Close Acaia apps and disable phone Bluetooth temporarily.
Check the Lunar Bluetooth connection indicator; if it is lit, another app/device may be connected.
Power-cycle the Lunar.
If it still does not appear, enter Lunar settings and confirm Bluetooth Setup Setting [bt_SEt] is On.
Then rerun tools/dev/monitor_lunar_poc.sh /dev/cu.usbserial-10.
```

## Step 11 - First Lunar Weight Stream

User reported:

```text
Lunar went off automatically, then was turned on again.
```

Observed behavior:

```text
ESP32 connected to the Lunar again.
Heartbeats were sent successfully.
Battery notifications decoded as 24%.
```

Raw BLE discovery:

```text
The Lunar emits complete logical frames across multiple BLE notifications.
Example split:
  notification 1: EF DD 0C
  notification 2: 0C 05 07 00 00 00 01 00 07 00 00 02 1B 07
Reassembled frame:
  EF DD 0C 0C 05 07 00 00 00 01 00 07 00 00 02 1B 07
```

Firmware response:

```text
Added a small byte reassembly buffer for Lunar BLE notifications.
The decoder now runs only after a complete EF DD frame is buffered.
The buffer is reset on connect/disconnect.
```

Live monitor evidence:

```text
Log file: data/raw/m1-lunar-ble/lunar-poc-20260731-223746.log
CSV weight samples: 189
First samples:
  11334,0.70,24,1
  11527,0.70,24,1
  11740,0.70,24,1
Last sample:
  51039,0.80,24,1
Observed weight range: 0.70 g to 1.00 g
Battery: 24%
```

Result:

```text
M1 proof of data passed at the first technical level.
ESP32 WROVER reads Acaia Lunar BLE frames, reassembles fragmented notifications, decodes weight, and emits CSV over USB serial.
```

Next engineering action:

```text
Turn payload debug off for normal runs.
Run a longer 10-minute capture with real tare and a simulated shot profile.
```

## Step 12 - 10-Minute Stable Weight Capture

User reported:

```text
gewicht liegt auf
```

Capture setup:

```text
Board: Freenove ESP32 WROVER
Port: /dev/cu.usbserial-10
Lunar: ACAIAL-C1EF962 / 00:1c:97:1e:f9:62
Payload debug: off
Scan debug: off
Log file: data/raw/m1-lunar-ble/lunar-poc-20260731-224741.log
```

Validation:

```text
Build before flash: freenove_esp32_wrover SUCCESS
Flash: SUCCESS
Monitor stopped manually after the 10-minute target was exceeded.
```

Live monitor evidence:

```text
CSV weight samples: 2843
First sample: 5801,100.00,22,1
Last sample: 618475,100.40,22,1
Capture duration: 612.674 s
Approx sample rate: 4.64 Hz
Observed weight range: 100.00 g to 100.50 g
Mean weight: 100.2243 g
Weight standard deviation: 0.1415 g
Battery: 22% across all samples
Connection flag: 1 across all samples
Error/disconnect scan: no matching lines
```

Result:

```text
M1 long capture passed.
The ESP32 WROVER maintained a stable BLE data stream from the Acaia Lunar for more than 10 minutes and produced parseable CSV weight data throughout the run.
```

Next engineering action:

```text
Add a small host-side log analyzer so future captures can be validated with one command.
Then run a dynamic shot-profile test: tare, start from 0 g, add weight gradually, verify monotonic flow-like data.
```

## Step 13 - Host-Side Capture Analyzer

Tool added:

```text
tools/dev/analyze_lunar_capture.py
```

Validation command:

```bash
tools/dev/analyze_lunar_capture.py --m1-long
```

Analyzer result on the 10-minute capture:

```text
result: PASS
samples: 2843
duration_s: 612.674
sample_rate_hz: 4.64
first_sample: 5801,100.00,22,1
last_sample: 618475,100.40,22,1
weight_g: min=100.00 max=100.50 mean=100.2243 stddev=0.1415
sample_gap_ms: max=363 mean=215.6 non_positive=0
battery_counts: 22:2843
connection_counts: 1:2843
error_lines: 0
criteria: {"max_gap_ms": 5000, "min_duration_sec": 600, "min_samples": 1000, "require_connected": true}
```

Result:

```text
M1 long-capture validation is now repeatable with one host-side command.
```

## Step 14 - Dynamic Shot-Profile Dry Run

User reported:

```text
leer und bereit
```

Capture setup:

```text
Board: Freenove ESP32 WROVER
Port: /dev/cu.usbserial-10
Lunar: ACAIAL-C1EF962 / 00:1c:97:1e:f9:62
Payload debug: off
Scan debug: off
Log file: data/raw/m1-lunar-ble/lunar-poc-20260731-232829.log
```

Baseline:

```text
The stream was stable at 0.00 g before the pour.
Battery: 17%
Connection: active
```

General analyzer result:

```text
result: PASS
samples: 439
duration_s: 100.337
sample_rate_hz: 4.38
first_sample: 2454443,0.00,17,1
last_sample: 2554780,0.00,17,1
weight_g: min=-0.10 max=159.30 mean=6.4565 stddev=16.4280
sample_gap_ms: max=1201 mean=229.1 non_positive=0
battery_counts: 17:439
connection_counts: 1:439
error_lines: 0
```

Dynamic segment:

```text
First rise over 1 g: 2510422,3.50,17,1
Reached 30 g: 2512088,30.00,17,1
Reached 35 g: 2512747,36.10,17,1
Time from first rise to 30 g: 1.666 s
Time from first rise to 35 g: 2.325 s
Initial plateau around 35.3 g to 35.6 g was visible after the rise.
```

Artifacts observed:

```text
Large jumps after the first plateau:
  35.30 g to 77.00 g
  42.20 g to 159.30 g
  130.70 g to 0.00 g
  -0.10 g to 72.90 g
These are consistent with physical handling, vessel movement, or removing/placing weight during the capture.
```

Result:

```text
Dynamic weight acquisition passed.
Espresso-like shot-profile validation did not pass because the simulated pour reached 35 g in 2.325 s instead of a realistic 25-35 s window and contained post-plateau handling artifacts.
```

Next engineering action:

```text
Repeat the dynamic shot-profile test with the vessel left on the scale.
Start at 0.00 g, pour slowly to 35 g over 25-35 s, then stop pouring and leave the vessel untouched for 10 s.
```
